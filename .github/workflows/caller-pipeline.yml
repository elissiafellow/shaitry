name: Shai-Hulud 2 Supply Chain Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize]

jobs:
  scan:
    uses: elissiafellow/shaitry/.github/workflows/shai-hulud-2-scanner.yml@npm-scanner
    with:
      node_version: "18"
      # Scanner will recursively find all lockfiles in the repo
      # No need to specify scan_target - it defaults to root and scans everything
